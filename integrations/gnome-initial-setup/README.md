# GNOME Initial Setup integration

This directory contains integration assets derived from
[GNOME Initial Setup](https://gitlab.gnome.org/GNOME/gnome-initial-setup), such
as the session, D-Bus service, dconf defaults, and system-user definitions.
They let Ready Set run in the GNOME Initial Setup environment; application code
and Ready Set plugins live elsewhere in the repository.
