#%PAM-1.0
# GDM PAM configuration used only for the greeter session
auth		required	pam_succeed_if.so audit quiet_success user ingroup gnome-initial-setup
auth		required	pam_env.so
auth		required	pam_permit.so
account		required	pam_succeed_if.so audit quiet_success user ingroup gnome-initial-setup
account		required	pam_permit.so
password	required	pam_deny.so
session		required	pam_succeed_if.so audit quiet_success user ingroup gnome-initial-setup
session		substack	common-login
session		required	pam_permit.so
