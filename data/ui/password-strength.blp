using Gtk 4.0;

template $ReadySetPasswordStrength: Box {
  orientation: vertical;

  ProgressBar progress_bar {
    margin-end: 6;
    margin-start: 6;
  }

  $ReadySetMarginLabel {
    label: bind template.label;
  }
}
