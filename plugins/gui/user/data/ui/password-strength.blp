using Gtk 4.0;

template $UserPasswordStrength: Box {
  orientation: vertical;

  ProgressBar progress_bar {
    margin-end: 6;
    margin-start: 6;
  }

  $UserMarginLabel {
    label: bind template.label;
  }
}
