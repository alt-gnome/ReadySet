# Welcome plugin

Display a welcome page.

The `welcome` plugin disables the `language` plugin so that two welcome pages are not shown. You may still want to use the `language` page for language selection.
