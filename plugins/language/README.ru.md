# Плагин языка

Устанавливает локаль системы через `org.freedesktop.locale1`. Это шаг
`Welcome`, поэтому он может быть первой страницей сценария Ready Set.

## Сохраняемые значения контекста

| Переменная | Описание |
| --- | --- |
| `language.locale` | Выбранная локаль |
